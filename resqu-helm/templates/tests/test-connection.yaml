apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resqu.fullname" . }}-test-connection"
  labels:
    {{- include "resqu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resqu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
